# ⏸️ Bits / Binário — PAUSADO por decisão do aluno (2026-07-06)

**Não retomar por iniciativa própria.** O aluno decidiu estudar a base de
binário **por fora, com um livro**, no ritmo dele. Esperar ele sinalizar.

## O que aconteceu
Ele **domina a base intuitiva** (`%2`, `/2`, potências de 2, converter para
binário na mão — tudo AC). Travou nos **operadores bit a bit** (`&`, `|`, `^`,
`<<`, `>>`, `x&1`, `x>>1`): disse que "está grego", faltava base fundamental.

**Decisão madura — respeitar.** Não é fraqueza: é lacuna de base, e ele está
agindo certo ao buscá-la antes de seguir.

| Arquivo | Conteúdo |
|---------|----------|
| `teoria_bits.cpp` | Teoria dos operadores + B1/B2 (não concluídos) |

Exercícios: `revisoes/2026-07-06_bits_A1-A8_PAUSADO.cpp` (A1–A7 ✅, A8 referência).

## Pendente por causa disso
`codeforces/CF_popcount_PENDENTE_precisa_bits.cpp` — é guloso por camadas de
bits. A estratégia já foi validada por força bruta; falta a base de bits.
