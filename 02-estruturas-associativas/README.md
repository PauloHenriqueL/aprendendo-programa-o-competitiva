# 🗺️ Estruturas Associativas

`votacao.cpp` — apuração de votos: conta votos por nome com `map<string,int>`,
lista em ordem alfabética (o map já mantém a ordem) e acha o vencedor
(desempate alfabético). Introdução ao `map`.
